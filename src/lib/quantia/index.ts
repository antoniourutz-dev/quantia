/** Domain constants and shared types. API functions remain in {@link ../quantiaApi}. */
export { DEFAULT_CURRICULUM } from './constants';
export type { DashboardBundle, CurriculumOption } from './types';
